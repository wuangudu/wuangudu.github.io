name: Deploy VitePress site to Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install
        working-directory: Project-ATRI-Docs-main   # ← 关键：进入这个文件夹

      - name: Build
        run: npm run docs:build
        working-directory: Project-ATRI-Docs-main   # ← 关键：进入这个文件夹

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: Project-ATRI-Docs-main/docs/.vitepress/dist   # ← 输出路径也相应修改

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
